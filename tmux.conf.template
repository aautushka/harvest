# harvest tmux binding. __BIN__ is substituted by install.sh with the absolute binary path.

bind-key f run-shell 'selected=$(tmux capture-pane -p -t "#{pane_id}" -S -10000 | __BIN__ | fzf-tmux -p --reverse) && [ -n "$selected" ] && tmux send-keys -t "#{pane_id}" "$selected"'
